import string
import random

n = input("How strong you want the password to be range of 1-10")
for i in range(n):
    password=""
    num=random.randint(0,99999)
    num=str(num)
    password+=nums

splchar=['[', ']', '(', ')', '&', '*', '#', '@', '!', '$', '^']
for i in range(n):
    spl= Random(splchar)
    password+=spl
    
